# Sample leaked config -- the classic "committed the .env file" mistake.
# AttestorVonLuneberg flags the secrets even though this is not source code.
APP_NAME=billing-service
LOG_LEVEL=info

# hardcoded-secret: an unquoted production password.
DB_PASSWORD=Sup3rSecret_Pr0d_Passw0rd

# hardcoded-secret: an AWS access key id (this is AWS's well-known fake example).
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE

# Correct pattern (NOT flagged): value comes from the environment.
SESSION_SECRET=${SESSION_SECRET}
